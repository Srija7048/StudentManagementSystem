import { StudentDirective } from './student.directive';

describe('StudentDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentDirective();
    expect(directive).toBeTruthy();
  });
});
